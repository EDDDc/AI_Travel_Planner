server:
  port: 8080

spring:
  application:
    name: ai-travel-planner

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  ai:
    bailian:
      apiKey: ${BAILIAN_API_KEY:}
      baseUrl: ${BAILIAN_API_BASE:https://dashscope.aliyuncs.com/compatible-mode/v1}
      model: ${BAILIAN_MODEL:qwen-turbo}
  asr:
    xfyun:
      appId: ${XF_APP_ID:}
      apiKey: ${XF_API_KEY:}
      apiSecret: ${XF_API_SECRET:}
  map:
    amap:
      key: ${AMAP_KEY:}
